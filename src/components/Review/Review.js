import React from 'react';

const Review = ({review}) => {

    return (
        <div>
            {
                console.log(review.name)
            }
        </div>
    );
};

export default Review;